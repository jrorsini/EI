import React from 'react';

const EntryList = ({ entries }) => {
	console.log(entries);
	return (
		<div className="container">
			<ul>
				<li>First Entry</li>
			</ul>
		</div>
	);
};

export default EntryList;
